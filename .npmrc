# sqlite3 安装走预编译二进制（prebuild-install），避免回退到 node-gyp 源码编译。
# 背景：sqlite3@5.x 的 install 脚本为 `prebuild-install -r napi || node-gyp rebuild`，
# 默认从 GitHub Releases 拉预编译包；国内网络常超时而回退源码编译，
# 而源码编译需要 Windows SDK（本机 VS2022 仅装了 VC++ 工具链，缺 SDK，必失败）。
# 下面把 prebuild-install 的下载源指向 npmmirror 镜像，使每次 npm install 都直接拿预编译二进制。
sqlite3_binary_host_mirror=https://npmmirror.com/mirrors/sqlite3/
